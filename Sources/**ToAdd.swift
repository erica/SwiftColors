/*
 Add the thing that normalizes hues, then creates hashed buckets
 of colors for quicker lookups, then output those hashed element
 dictionaries in a reusable form so it can be loaded at start without
 using the Color+Dictionary files, which can then be moved elsewhere.
 */
